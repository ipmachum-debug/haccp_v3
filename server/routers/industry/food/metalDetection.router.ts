/**
 * 식품 금속 검출 라우터 — Layer 4 industry/food (CP-4 3단계)
 * 점진 이주 (Strangler Fig) — re-export 만.
 */
export { metalDetectionRouter as foodMetalDetectionRouter } from "../../haccp/metalDetection.router";
