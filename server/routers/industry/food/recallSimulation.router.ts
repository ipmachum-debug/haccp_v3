/**
 * 식품 회수 시뮬레이션 라우터 — Layer 4 industry/food (CP-4 5단계)
 * 점진 이주 (Strangler Fig) — re-export 만.
 */
export { recallSimulationRouter as foodRecallSimulationRouter } from "../../haccp/recall.router";
