/**
 * 식품 시정조치 (CAR) 라우터 — Layer 4 industry/food (CP-4 5단계)
 * 점진 이주 (Strangler Fig) — re-export 만.
 */
export { correctiveActionRouter as foodCorrectiveActionRouter } from "../../haccp/correctiveAction.router";
